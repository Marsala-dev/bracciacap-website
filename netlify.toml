[build]
  publish = "dist"
  command = "npm run build"

# Form submission redirect
[[redirects]]
  from = "/success"
  to = "/success.html"
  status = 200

# SPA fallback (must be last)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
